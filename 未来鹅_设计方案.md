
# 「未来鹅」大学生职业成长AI陪伴体 — 设计方案



---

## 一、问题诊断

| 痛点 | 现状 | 根因 |
|------|------|------|
| 中低年级学生缺少系统认知入口 | 校招信息主要面向高年级，单向推送 | 缺乏分阶段、主动触达的连接方式 |
| 求职信息差大 | 公众号/宣讲会信息碎片化，难以个性化 | 缺少一对一、持续性的信息整合 |
| 缺乏情感链接 | 企业与学生关系停留在"招聘-应聘" | 缺少陪伴式、成长导向的长期交互 |
| 各年级需求差异大 | 同一套内容打天下 | 缺乏年级感知和自适应路由 |

**核心洞察**：学生需要的不是"招聘信息检索器"，而是"一个懂我阶段、陪我成长的AI学长/学姐"。

---

## 二、方案设计

### 2.1 产品定位

> 「未来鹅」= 你的腾讯AI学长，从大一到毕业全程陪伴，帮你搞清楚"去哪、做什么、怎么准备"

### 2.2 系统架构

```
┌─────────────────────────────────────────────────┐
│                   用户界面层                      │
│            Streamlit Web Demo                    │
├─────────────────────────────────────────────────┤
│                   编排层（LangGraph）              │
│                                                  │
│   ┌──────────┐    ┌──────────────────────────┐  │
│   │  Router   │───▶│  Grade-Aware Routing     │  │
│   │  Agent    │    │  (大一/大二/大三/大四+)   │  │
│   └──────────┘    └──────────────────────────┘  │
│         │              │         │         │     │
│    ┌────┴────┐   ┌─────┴──┐ ┌────┴───┐ ┌───┴────┐
│    │探索向导  │   │实习导航│ │求职教练│ │面试陪练│
│    │Explorer │   │Intern  │ │Coach   │ │Mock    │
│    │ Agent   │   │ Agent  │ │ Agent  │ │ Agent  │
│    └─────────┘   └────────┘ └────────┘ └────────┘
│                                                  │
│   ┌──────────────────────────────────────────┐   │
│   │         Memory & Context Layer           │   │
│   │    (年级/专业/兴趣/对话历史/成长档案)     │   │
│   └──────────────────────────────────────────┘   │
├─────────────────────────────────────────────────┤
│                   工具层                          │
│   ┌────────┐ ┌──────────┐ ┌────────┐ ┌───────┐  │
│   │知识检索 │ │岗位查询  │ │简历分析│ │模拟面试│  │
│   │RAG     │ │Job Search│ │Resume  │ │Mock   │  │
│   └────────┘ └──────────┘ └────────┘ └───────┘  │
├─────────────────────────────────────────────────┤
│                   模型层                          │
│         LLM API（本地部署时填入Key）               │
└─────────────────────────────────────────────────┘
```

### 2.3 四大Agent职责

| Agent | 目标年级 | 核心能力 | 典型对话 |
|-------|---------|---------|---------|
| **探索向导** | 大一/大二 | 行业认知、职业地图、兴趣探索 | "互联网有哪些岗位？产品经理是做什么的？" |
| **实习导航** | 大二/大三 | 实习准备、鹅厂岗位匹配、投递指导 | "大三想找产品实习，该怎么准备？" |
| **求职教练** | 大四/研究生 | 简历优化、求职策略、时间线规划 | "秋招时间线怎么安排？简历怎么改？" |
| **面试陪练** | 所有年级 | 模拟面试、行为题/Skill题训练、反馈 | "帮我模拟一场产品岗群面" |

### 2.4 核心交互流程

```
用户进入 → 选择年级/专业 → Router Agent路由
    ↓
进入对应Agent → 系统Prompt引导角色定位
    ↓
多轮对话 → Memory记录偏好/进度 → 工具调用（检索/分析/模拟）
    ↓
结构化输出 → 个性化建议 + 行动清单 + 鹅厂信息自然融入
```

---

## 三、技术选型

### 3.1 框架与工具

| 组件 | 选型 | 理由 |
|------|------|------|
| Agent编排 | **LangGraph** | 原生支持状态机、条件路由、循环，比纯LangChain更适合多Agent协作 |
| LLM集成 | **LangChain** | 成熟的模型抽象层，支持OpenAI/DeepSeek/本地模型无缝切换 |
| 知识检索 | **LangChain RAG + ChromaDB** | 轻量向量库，适合Demo级部署 |
| 后端 | **FastAPI** | 异步高性能，自动生成API文档 |
| 前端 | **Streamlit** | Python原生，快速搭建对话UI，支持部署到Streamlit Cloud |
| 部署 | **Streamlit Community Cloud** | 免费、公网可访问、支持Secret管理 |

### 3.2 GitHub Skills/工具集成

| 工具 | 来源 | 用途 |
|------|------|------|
| langchain-tools | github.com/langchain-ai | Web搜索、文档加载等基础工具 |
| duckduckgo-search | github.com/deedy5/duckduckgo_search | 无需API Key的搜索，替代Tavily |
| chromadb | github.com/chroma-core/chroma | 向量存储，RAG知识库 |
| streamlit-chat | github.com/AI-Yash/st-chat | 对话UI组件 |

### 3.3 系统Prompt设计（参考Claude Code范式）

采用Claude Code式的分层系统Prompt架构：

```python
SYSTEM_PROMPT = """
# 身份与定位
你是「未来鹅」，腾讯校招团队的AI学长/学姐。你的核心使命是陪伴大学生从大一到毕业的职业成长旅程，而不是单纯的招聘工具。

# 核心原则
1. 陪伴优先：先理解学生所处阶段和困惑，再给建议
2. 真实帮助：每个建议必须可执行，不说空话套话
3. 自然融入：鹅厂信息作为"学长经验"自然分享，不做硬广
4. 阶段感知：根据年级自动调整对话深度和建议策略
5. 情感链接：承认焦虑、鼓励探索、庆祝进步

# 年级路由策略
- 大一：探索为主，开放性问题引导，建立职业认知框架
- 大二：聚焦专业与职业关联，引导实习意识，能力盘点
- 大三：实习实战指导，简历/作品集准备，鹅厂岗位匹配
- 大四/研究生：求职冲刺，面试训练，时间线管理，offer决策

# 对话规范
- 回答简洁自然，像微信聊天，不说教
- 每次回答结尾给出1-2个具体行动项
- 涉及鹅厂信息时标注来源
- 不确定的信息坦诚说明，不编造
- 发现学生情绪低落时，先共情再给建议

# 工具使用
- 当学生问岗位信息时，调用岗位知识库检索
- 当学生要求改简历时，调用简历分析工具
- 当学生要求模拟面试时，切换到面试陪练模式

# 安全边界
- 不透露腾讯内部机密信息
- 不对薪资做具体承诺
- 不替代专业心理咨询
- 遇到超出能力范围的问题，建议咨询学校就业指导中心
"""
```

---

## 四、项目结构

```
weilai-e/
├── app.py                    # Streamlit主入口
├── config/
│   ├── settings.py           # 配置管理（API Key等）
│   └── prompts.py            # 系统Prompt定义
├── agents/
│   ├── router.py             # 路由Agent（LangGraph StateGraph）
│   ├── explorer.py           # 探索向导Agent
│   ├── intern.py             # 实习导航Agent
│   ├── coach.py              # 求职教练Agent
│   └── mock_interview.py     # 面试陪练Agent
├── graph/
│   └── workflow.py           # LangGraph工作流定义
├── tools/
│   ├── knowledge_search.py   # RAG知识检索工具
│   ├── resume_analyzer.py    # 简历分析工具
│   └── job_matcher.py        # 岗位匹配工具
├── data/
│   ├── knowledge_base/       # 鹅厂校招知识文档
│   │   ├── tencent_culture.md
│   │   ├── job_descriptions.md
│   │   ├── internship_guide.md
│   │   └── interview_tips.md
│   └── vector_store/         # ChromaDB向量存储
├── memory/
│   └── conversation.py       # 对话记忆管理
├── requirements.txt
├── .env.example              # 环境变量模板（API Key留空）
├── .streamlit/
│   └── secrets.toml.example  # Streamlit密钥模板
└── README.md
```

---

## 五、关键配置说明

### 5.1 LangGraph工作流

```python
from langgraph.graph import StateGraph, END

# 状态定义
class ChatState(TypedDict):
    messages: list
    grade: str          # 年级
    major: str          # 专业
    agent: str          # 当前路由到的Agent
    memory: dict        # 用户成长档案

# 工作流
workflow = StateGraph(ChatState)
workflow.add_node("router", route_by_grade)
workflow.add_node("explorer", explorer_agent)
workflow.add_node("intern", intern_agent)
workflow.add_node("coach", coach_agent)
workflow.add_node("mock", mock_interview_agent)

workflow.add_conditional_edges("router", lambda s: s["agent"], {
    "explorer": "explorer",
    "intern": "intern",
    "coach": "coach",
    "mock": "mock"
})
# 每个Agent完成后回到router，支持动态切换
for agent in ["explorer", "intern", "coach", "mock"]:
    workflow.add_edge(agent, "router")

workflow.set_entry_point("router")
```

### 5.2 API Key管理策略

```python
# config/settings.py
import os
from dotenv import load_dotenv

load_dotenv()

class Settings:
    # 留空，仅本地部署时填写
    LLM_API_KEY: str = os.getenv("LLM_API_KEY", "")
    LLM_BASE_URL: str = os.getenv("LLM_BASE_URL", "https://api.openai.com/v1")
    LLM_MODEL: str = os.getenv("LLM_MODEL", "gpt-4o-mini")
    
    # 支持通过Streamlit Secrets注入
    @classmethod
    def from_streamlit_secrets(cls):
        try:
            import streamlit as st
            return cls(
                LLM_API_KEY=st.secrets.get("LLM_API_KEY", ""),
                LLM_BASE_URL=st.secrets.get("LLM_BASE_URL", cls.LLM_BASE_URL),
            )
        except:
            return cls()
```

---

## 六、迭代计划

| 阶段 | 时间 | 目标 | 交付 |
|------|------|------|------|
| V0.1 | Day1 | 基础对话 + 路由 | 单Agent + 年级选择 + 基本对话 |
| V0.2 | Day2 | LangGraph多Agent | 4个Agent + 条件路由 + 工具调用 |
| V0.3 | Day3 | RAG知识库 | 鹅厂知识文档 + 向量检索 + 引用标注 |
| V0.4 | Day4 | 记忆与个性化 | 对话记忆 + 成长档案 + 跨会话连续性 |
| V1.0 | Day5 | 部署上线 | Streamlit Cloud部署 + 方案文档 + 演示 |

---

## 七、效果评估

| 指标 | 度量方式 | 目标 |
|------|---------|------|
| 回答相关性 | 人工评估5分制 | ≥4.0 |
| 年级路由准确率 | 路由结果vs预期 | ≥90% |
| 鹅厂信息融入自然度 | 人工评估5分制 | ≥3.5 |
| 行动项可执行性 | 人工评估5分制 | ≥4.0 |
| Demo可用性 | 公网可访问、对话流畅 | 通过 |

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
